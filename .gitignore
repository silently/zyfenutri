__pycache__/
*.py[cod]
.venv/
.pytest_cache/
dist/
*.egg-info/

# Documentation de travail : des PDF de référence (guides de la Commission,
# tables) qu'on n'a pas le droit de redistribuer. Ils restent sur le disque,
# jamais dans le dépôt.
docs/

.DS_Store

# WEB (`web/`) — cf. CLAUDE.md § CORE et WEB
web/node_modules/
web/.svelte-kit/
web/build/
# ⚠️ Copie de CORE fabriquée par `scripts/sync-core.mjs` à chaque dev/build.
# La versionner créerait une SECONDE source des règles, qui divergerait —
# exactement ce que la frontière CORE / WEB interdit.
web/static/core/
