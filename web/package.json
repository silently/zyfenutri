{
  "name": "zyfenutri-web",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-core": "node scripts/sync-core.mjs",
    "predev": "npm run sync-core",
    "dev": "vite dev",
    "prebuild": "npm run sync-core",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run sync-core && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@lucide/svelte": "^1.18.0",
    "@sveltejs/adapter-static": "^3.0.0",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tailwindcss/vite": "^4.3.3",
    "daisyui": "^5.7.28",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.5.0",
    "vite": "^8.0.0",
    "yaml": "^2.6.0"
  }
}
