// ⚠️ Page STATIQUE et entièrement cliente : le calcul tourne dans le
// navigateur (Pyodide), il n'y a donc rien à rendre côté serveur — et aucun
// serveur pour le faire. Cf. CLAUDE.md § CORE et WEB.
export const prerender = true;
export const ssr = false;
